# Independent candidate implementation path

This directory contains a deliberately table-driven projection implementation that does not import ARPA reference-service protocol processing code. It is included to test independent interpretation of the projection rules. Both implementations share only public JSON fixtures and the documented mapping contract. This is repository-supplied independence evidence, not external implementation evidence.
