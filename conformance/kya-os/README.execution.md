# Execution note

The profile is exercised by the repository `validate` target and its focused pytest coverage. No separate workflow is introduced; this keeps the capability inside ARPA's canonical completion gate.
