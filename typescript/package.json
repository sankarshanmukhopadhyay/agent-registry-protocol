{
  "name": "arpa-typescript-implementation",
  "version": "0.1.0",
  "private": true,
  "description": "Independent TypeScript implementation track for ARPA v0.9.4 normative artifacts and conformance vectors.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "conformance": "npm run build && node dist/src/conformance.js",
    "validate:examples": "npm run build && node dist/src/validate-examples.js",
    "release-check": "npm run typecheck && npm test && npm run validate:examples && npm run conformance"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
